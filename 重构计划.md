# ICT 实验室平台重构计划

---

## ⚠️ 重要提醒：部署架构（禁止修改）

> **以下部署配置已完成并正常运行，重构过程中不得修改！**

### 当前部署架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        代码仓库                                  │
│  ┌─────────────────┐         ┌─────────────────┐               │
│  │    Gitee        │  同步    │    GitHub       │               │
│  │  (主仓库)        │ ──────► │  (部署源)        │               │
│  └─────────────────┘         └─────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        部署平台                                  │
│  ┌─────────────────┐         ┌─────────────────┐               │
│  │    Vercel       │         │    Render       │               │
│  │  (前端部署)      │         │  (后端部署)      │               │
│  │  从 GitHub 自动  │         │  从 GitHub 自动  │               │
│  │  构建部署        │         │  构建部署        │               │
│  └─────────────────┘         └─────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      域名与网络                                  │
│  ┌─────────────────┐         ┌─────────────────┐               │
│  │  Cloudflare     │         │   购买的域名     │               │
│  │  (CDN + DNS)    │ ◄─────► │                 │               │
│  │  国内访问加速    │         │                 │               │
│  └─────────────────┘         └─────────────────┘               │
└─────────────────────────────────────────────────────────────────┘
```

### 部署流程（保持不变）

```
本地开发 → Gitee 推送 → GitHub 同步 → Vercel/Render 自动构建部署
```

### 🚫 禁止修改的项目

| 项目 | 说明 | 状态 |
|------|------|------|
| Gitee 仓库地址 | 主代码仓库 | ✅ 已完成 |
| GitHub 仓库地址 | 部署源仓库 | ✅ 已完成 |
| Vercel 前端部署配置 | 自动从 GitHub 构建 | ✅ 已完成 |
| Render 后端部署配置 | 自动从 GitHub 构建 | ✅ 已完成 |
| Cloudflare DNS 配置 | 域名解析 | ✅ 已完成 |
| 域名绑定 | 自定义域名 | ✅ 已完成 |
| CORS 配置 | 后端已配置允许前端域名 | ✅ 已完成 |
| 环境变量 | 前后端环境变量 | ✅ 已完成 |

### ✅ 本次重构范围

| 项目 | 是否修改 | 说明 |
|------|----------|------|
| 前端代码 | ✅ 修改 | 添加页面和组件 |
| 后端代码 | ✅ 修改 | 添加 API 和数据模型 |
| 部署配置 | ❌ 不修改 | 保持现有自动部署 |
| 域名配置 | ❌ 不修改 | 保持现有配置 |
| 环境变量 | ❌ 不修改 | 保持现有配置 |

### 更新部署的步骤

```
1. 本地开发完成后，代码推送到 Gitee
2. 同步到 GitHub
3. Vercel 和 Render 自动检测更新并重新构建
4. 构建完成后刷新网页即可看到更新
```

---

## 一、项目背景

### 1.1 当前项目结构

```
ict-lab-platform/
├── frontend/          # React 前端项目 (实验室管理系统)
├── backend/           # Express 后端项目
├── Mizuki-master/     # Astro 博客模板 (作为 UI 参考)
├── 首页ui.html        # 新设计的门户首页组件
└── README.md
```

### 1.2 技术栈总览

| 模块 | 框架 | 样式方案 | 部署平台 |
|------|------|----------|----------|
| 统一前端 | React 18 + Vite | Tailwind CSS + Ant Design | Vercel |
| 统一后端 | Express + Prisma | - | Render |

> **重要变更**：个人博客将集成到现有的 React 前端项目中，与实验室系统共用同一套前后端部署，无需额外部署。

---

## 二、改造目标

### 2.1 整体架构

> **统一部署方案**：博客功能集成到现有 React 项目中，与实验室系统共用前端和后端。

```
┌──────────────────────────────────────────────────────────────┐
│                         门户首页 (/)                          │
│              [网站简介] + [实验室入口] + [博客入口]              │
├────────────────────────────┬─────────────────────────────────┤
│                            │                                 │
│     实验室系统 (/lab)        │      个人博客 (/blog)            │
│  ┌──────────────────────┐   │  ┌───────────────────────────┐  │
│  │ 器材借阅大厅          │   │  │ 博客文章列表              │  │
│  │ 共享资料库            │   │  │ 文章详情页                │  │
│  │ 讨论区                │   │  │ 关于我                    │  │
│  │ 教程笔记              │   │  │ 项目展示                  │  │
│  │ 办公室规划            │   │  │ 友链页面                  │  │
│  │ 管理员后台            │   │  │ 归档页面                  │  │
│  └──────────────────────┘   │  └───────────────────────────┘  │
│                            │                                 │
│                    ┌───────┴────────┐                        │
│                    │   共享后端 API   │                        │
│                    │  (Express+Prisma) │                       │
│                    └────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 路由规划

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | 门户首页 | 新设计的入口页面 |
| `/login` | 登录页 | 用户登录 |
| `/register` | 注册页 | 用户注册 |
| `/lab` | 实验室首页 | 原实验室系统首页 |
| `/lab/equipment` | 器材借阅 | 实验室功能页 |
| `/lab/resources` | 共享资料 | 实验室功能页 |
| `/lab/forum` | 讨论区 | 实验室功能页 |
| `/lab/tutorials` | 教程笔记 | 实验室功能页 |
| `/lab/office` | 办公室规划 | 实验室功能页 |
| `/lab/admin/*` | 管理后台 | 管理员功能页 |
| `/blog` | 博客首页 | **React 实现** |
| `/blog/post/:id` | 文章详情 | **React 实现** |
| `/blog/about` | 关于我 | **React 实现** |
| `/blog/projects` | 项目展示 | **React 实现** |
| `/blog/archive` | 归档页 | **React 实现** |

---

## 三、UI 界面设计

### 3.1 门户首页设计 (基于首页ui.html)

#### 整体风格：明日方舟 (Arknights) 工业科技风

门户首页采用了游戏《明日方舟》官网的设计语言，特点是：
- **工业感**：冷色调、蓝图背景、网格线条
- **科技感**：终端数据流、系统监控装饰、加载动画
- **极简主义**：切角按钮、干净的排版、大量留白

#### 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  ┌─────────────────────────────────────────────────────┐   │
│  │                    LOADING 屏幕                       │   │
│  │         "WORKSPACE" 大字 + 进度条动画                  │   │
│  │              自动消失后显示主界面                      │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 顶部导航栏                                            │   │
│  │ [WORKSPACE] [INDEX] [OVERVIEW] [LABORATORY] [ARCHIVES]│   │
│  │             首页    全部系统    实验室      个人博客   │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                       │   │
│  │              主内容区 (蓝图背景)                        │   │
│  │                                                       │   │
│  │   ┌─────────────────────────────────────────────┐    │   │
│  │   │                                             │    │   │
│  │   │   "WORKSPACE" 超大字水印                      │    │   │
│  │   │                                             │    │   │
│  │   │   网格背景 + 装饰圆圈                          │    │   │
│  │   │                                             │    │   │
│  │   │   左下角：站点标题 + 版权信息                   │    │   │
│  │   │                                             │    │   │
│  │   └─────────────────────────────────────────────┘    │   │
│  │                                                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │              右侧滑动抽屉面板                          │   │
│  │  ┌─────────────────────────────────────────────────┐│   │
│  │  │ OVERVIEW (全部系统)                              ││   │
│  │  │ ┌──────────────┐ ┌──────────────┐               ││   │
│  │  │ │  实验室系统   │ │  个人博客     │               ││   │
│  │  │ │  [进入]      │ │  [进入]       │               ││   │
│  │  │ └──────────────┘ └──────────────┘               ││   │
│  │  ├─────────────────────────────────────────────────┤│   │
│  │  │ LABORATORY (实验室详情)                          ││   │
│  │  │ - 数据中枢与归档                                 ││   │
│  │  │ - 高危样本管控                                   ││   │
│  │  ├─────────────────────────────────────────────────┤│   │
│  │  │ ARCHIVES (博客详情)                              ││   │
│  │  │ - 最新文章列表                                   ││   │
│  │  └─────────────────────────────────────────────────┘│   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 视觉元素详解

**1. Loading 屏幕**
```
┌────────────────────────────────────────────┐
│                                            │
│              WORKSPACE                     │
│           - SYSTEM ONLINE -                │
│                                            │
│                                            │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━   │
│  © HYPERGRYPH  » LOADING - 87% ......      │
│                WORKSPACE // https://...    │
└────────────────────────────────────────────┘
```
- 深灰背景 (#222222)
- 超大 TEKO 字体标题
- 底部进度条动画 (随机跳跃式增长)
- 版权信息和加载状态

**2. 主界面背景**
- 浅灰蓝图背景 (#d1d5db)
- 40px 网格线条
- 装饰性同心圆
- 斜线和垂直线装饰
- 左下角超大 "RHO" 水印

**3. 装饰元素**
```
左上角终端数据流:
┌─────────────────────────────┐
│ NODE_CONNECT: ESTABLISHED   │
│ SERVER_PING: 12ms           │
│ DATA_STREAM: ACTIVE █       │
│ 01001000 01100101...        │
└─────────────────────────────┘

右上角系统规格:
┌─────────────────────────────┐
│ PRTS_CORE_SYSTEM // V3.0.1  │
│ MEM_CAPACITY: 8192 TB       │
│ SECURITY_LEVEL: OMEGA       │
└─────────────────────────────┘
```

**4. 导航栏**
- 固定在顶部
- `mix-blend-difference` 效果 (自动适应背景色)
- 悬浮时青色高亮 (#00e5ff)
- 下划线动画指示当前页面

**5. 切角按钮**
```
┌─────────────────┐
│   ENTER SYSTEM  │  ◀── 切角设计
└─────────────────┘     右下角有青色方块装饰
```

#### 配色方案

| 变量 | 颜色值 | 用途 |
|------|--------|------|
| `--ark-cyan` | #00e5ff | 主强调色 (实验室相关) |
| `--ark-yellow` | #fde047 | 次强调色 (博客相关) |
| `--ark-dark` | #111111 | 深色背景 |
| `--ark-gray` | #1a1a1a | 卡片背景 |
| `--bg-light` | #d1d5db | 浅色图纸背景 |

#### 动画效果

1. **加载进度条** - 随机跳跃式增长
2. **面板滑入** - 贝塞尔曲线缓动 (0.77, 0, 0.175, 1)
3. **光标闪烁** - 模拟终端效果
4. **按钮扫光** - 悬浮时的切角装饰

---

### 3.2 实验室系统设计 (现有 Home.jsx)

实验室系统内部首页采用现代玻璃拟态设计：

#### 设计特点

- **深色主题**：#030712 深蓝黑背景
- **粒子背景**：Canvas 绘制的动态粒子网络
- **玻璃卡片**：毛玻璃效果 + 边框发光
- **3D 倾斜**：鼠标跟随的透视变换
- **滚动揭示**：IntersectionObserver 驱动的入场动画

#### 页面区块

1. **Hero 区域**
   - 粒子背景 + 流光球
   - 大标题 + 渐变文字
   - 日期显示 + 用户欢迎语
   - CTA 按钮

2. **数据统计卡片**
   - 器材种类、共享资料、讨论帖数、教程笔记
   - 计数器动画
   - 玻璃卡片效果

3. **研究方向卡片**
   - 人工智能与深度学习
   - 物联网与边缘计算
   - 大数据分析
   - 网络与信息安全

4. **核心赛事 + 打卡区域**
   - 赛事标签展示
   - 今日打卡功能

---

### 3.3 个人博客设计 (React 实现，参考 Mizuki 风格)

> **方案说明**：参考 Mizuki 模板的 UI 设计，使用 React + Tailwind CSS 重新实现博客功能，集成到现有项目中。

#### 设计特点

- **双栏布局**：左侧内容区 + 右侧小部件栏
- **响应式设计**：移动端自适应为单栏
- **代码高亮**：使用 Highlight.js (项目已有依赖)
- **Markdown 渲染**：使用 react-markdown (项目已有依赖)

#### 页面结构

```
┌──────────────────────────────────────────────────────────┐
│                        顶部导航                           │
│     [首页] [实验室] [博客]                    [主题切换]    │
├──────────────────────────────────────────────────────────┤
│                    Banner 横幅 (可选)                     │
├──────────────────────────────────────────────────────────┤
│                     │                                    │
│    侧边栏小部件      │          文章内容区                  │
│  ┌──────────────┐  │  ┌────────────────────────────────┐ │
│  │ 个人资料卡片   │  │  │                                │ │
│  │ 头像 + 简介   │  │  │   ┌──────────────────────────┐ │ │
│  ├──────────────┤  │  │   │ 文章卡片                  │ │ │
│  │ 文章分类      │  │  │   │ 标题 + 摘要 + 日期        │ │ │
│  │ - 技术       │  │  │   └──────────────────────────┘ │ │
│  │ - 生活       │  │  │   ┌──────────────────────────┐ │ │
│  │ - 随笔       │  │  │   │ 文章卡片                  │ │ │
│  ├──────────────┤  │  │   │ 标题 + 摘要 + 日期        │ │ │
│  │ 最新文章      │  │  │   └──────────────────────────┘ │ │
│  └──────────────┘  │  └────────────────────────────────┘ │
│                     │                                    │
└──────────────────────────────────────────────────────────┘
```

#### 博客页面列表

| 页面 | 路径 | 功能 | 优先级 |
|------|------|------|--------|
| 博客首页 | `/blog` | 文章列表 | 高 |
| 文章详情 | `/blog/post/:id` | Markdown 渲染 | 高 |
| 关于我 | `/blog/about` | 个人介绍 | 中 |
| 归档 | `/blog/archive` | 按时间归档 | 中 |
| 项目展示 | `/blog/projects` | 项目列表 | 低 |
| 友链 | `/blog/friends` | 友情链接 | 低 |

---

## 四、实施步骤

### 4.1 阶段一：前端改造

#### 步骤 1：创建门户首页组件

**文件**: `frontend/src/pages/Portal.jsx`

将 `首页ui.html` 转换为 React 组件：
- 导入 React Router 的 `Link` 和 `useNavigate`
- 配置导航跳转逻辑
- 保留原有样式和动画

#### 步骤 2：创建博客页面组件

**新建文件**:
```
frontend/src/pages/blog/
├── BlogHome.jsx      # 博客首页 (文章列表)
├── BlogPost.jsx      # 文章详情页
├── BlogAbout.jsx     # 关于我
├── BlogArchive.jsx   # 归档页
└── BlogLayout.jsx    # 博客布局组件
```

#### 步骤 3：创建博客组件

**新建文件**:
```
frontend/src/components/blog/
├── Sidebar.jsx       # 侧边栏 (个人信息、分类、最新文章)
├── PostCard.jsx      # 文章卡片
└── PostContent.jsx   # 文章内容渲染 (Markdown)
```

#### 步骤 4：修改路由配置

**文件**: `frontend/src/App.jsx`

```jsx
// 新路由结构
<Routes>
  {/* 公共页面 */}
  <Route path="/" element={<Portal />} />
  <Route path="/login" element={<Login />} />
  <Route path="/register" element={<Register />} />

  {/* 实验室系统 */}
  <Route path="/lab" element={<ProtectedRoute><Layout /></ProtectedRoute>}>
    <Route index element={<Home />} />
    <Route path="equipment" element={<Equipment />} />
    <Route path="resources" element={<Resources />} />
    <Route path="forum" element={<Forum />} />
    <Route path="tutorials" element={<Tutorials />} />
    <Route path="office" element={<Office />} />
    <Route path="admin/*" element={<AdminRoutes />} />
  </Route>

  {/* 个人博客 */}
  <Route path="/blog" element={<BlogLayout />}>
    <Route index element={<BlogHome />} />
    <Route path="post/:id" element={<BlogPost />} />
    <Route path="about" element={<BlogAbout />} />
    <Route path="archive" element={<BlogArchive />} />
  </Route>

  {/* 404 */}
  <Route path="*" element={<Navigate to="/" replace />} />
</Routes>
```

#### 步骤 5：更新布局组件

**文件**: `frontend/src/components/Layout.jsx`

- 更新侧边栏导航链接，添加 `/lab` 前缀
- 添加返回门户首页的入口

---

### 4.2 阶段二：后端改造

#### 步骤 1：添加博客数据模型

**文件**: `backend/prisma/schema.prisma`

```prisma
// 博客文章
model Article {
  id          String   @id @default(cuid())
  title       String
  slug        String   @unique
  content     String   // Markdown 内容
  excerpt     String?  // 摘要
  coverImage  String?  // 封面图
  category    String   // 分类
  tags        String?  // 标签 (逗号分隔)
  published   Boolean  @default(false)
  viewCount   Int      @default(0)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  authorId    String
  author      User     @relation(fields: [authorId], references: [id])
}

// 更新 User 模型，添加关联
model User {
  // ... 现有字段
  articles    Article[]
}
```

#### 步骤 2：创建博客 API

**新建文件**: `backend/src/routes/articles.js`

```javascript
// API 端点
GET    /api/articles          // 获取文章列表 (分页、分类筛选)
GET    /api/articles/:id      // 获取文章详情
POST   /api/articles          // 创建文章 (需登录)
PUT    /api/articles/:id      // 更新文章 (需登录)
DELETE /api/articles/:id      // 删除文章 (需登录)
GET    /api/articles/archive  // 获取归档数据
```

#### 步骤 3：注册路由

**文件**: `backend/src/index.js`

```javascript
import articlesRouter from './routes/articles';
app.use('/api/articles', articlesRouter);
```

#### 步骤 4：运行数据库迁移

```bash
cd backend
npx prisma migrate dev --name add_articles
```

---

### 4.3 阶段三：前端 API 集成

#### 创建 API 客户端

**新建文件**: `frontend/src/api/articles.js`

```javascript
import client from './client';

export const getArticles = (params) => client.get('/articles', { params });
export const getArticle = (id) => client.get(`/articles/${id}`);
export const createArticle = (data) => client.post('/articles', data);
export const updateArticle = (id, data) => client.put(`/articles/${id}`, data);
export const deleteArticle = (id) => client.delete(`/articles/${id}`);
```

---

### 4.4 阶段四：测试与优化

1. 测试所有路由跳转
2. 测试博客文章 CRUD
3. 测试 Markdown 渲染
4. 响应式布局测试

---

## 五、文件变更清单

### 5.1 前端新建文件

| 文件 | 说明 |
|------|------|
| `frontend/src/pages/Portal.jsx` | 门户首页 |
| `frontend/src/pages/blog/BlogHome.jsx` | 博客首页 |
| `frontend/src/pages/blog/BlogPost.jsx` | 文章详情 |
| `frontend/src/pages/blog/BlogAbout.jsx` | 关于页 |
| `frontend/src/pages/blog/BlogArchive.jsx` | 归档页 |
| `frontend/src/pages/blog/BlogLayout.jsx` | 博客布局 |
| `frontend/src/components/blog/Sidebar.jsx` | 侧边栏组件 |
| `frontend/src/components/blog/PostCard.jsx` | 文章卡片 |
| `frontend/src/api/articles.js` | 博客 API |

### 5.2 前端修改文件

| 文件 | 修改内容 |
|------|----------|
| `frontend/src/App.jsx` | 添加门户首页和博客路由 |
| `frontend/src/components/Layout.jsx` | 更新导航链接 |

### 5.3 后端新建文件

| 文件 | 说明 |
|------|------|
| `backend/src/routes/articles.js` | 博客 API 路由 |

### 5.4 后端修改文件

| 文件 | 修改内容 |
|------|----------|
| `backend/prisma/schema.prisma` | 添加 Article 模型 |
| `backend/src/index.js` | 注册文章路由 |

---

## 六、预期效果

### 用户体验流程

```
用户访问网站
    │
    ▼
┌─────────────────┐
│   Loading 屏幕   │  ← 品牌展示
│   WORKSPACE     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    门户首页      │  ← 网站简介
│  [实验室] [博客] │  ← 入口选择
└────────┬────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌───────┐ ┌───────┐
│实验室  │ │ 博客   │
│系统    │ │ 页面   │
│(需登录)│ │(公开)  │
└───────┘ └───────┘
```

### 技术优势

1. **统一部署** - 只需维护一套前后端代码
2. **共享认证** - 博客管理可复用现有用户系统
3. **数据统一** - 文章存储在同一个数据库
4. **样式一致** - 使用相同的 Tailwind + Ant Design 样式

---

## 七、时间预估

| 阶段 | 任务 | 预估时间 |
|------|------|----------|
| 1 | 门户首页 + 路由改造 | 1-2 小时 |
| 2 | 博客页面组件 | 2-3 小时 |
| 3 | 后端 API + 数据库 | 1 小时 |
| 4 | 样式调整与测试 | 1 小时 |
| **总计** | | **5-7 小时** |

---

## 八、风险与注意事项

1. **路由兼容性**
   - 确保原有 `/` 路径的重定向正确
   - 测试所有内部链接

2. **数据库迁移**
   - 本地测试后再推送到生产环境
   - 备份现有数据

3. **Markdown 安全**
   - 文章内容需要做 XSS 过滤
   - 使用已有的 react-markdown 配合 rehype-sanitize

4. **权限控制**
   - 博客查看：公开
   - 博客管理：需要登录 (可限制特定用户)

---

## 九、可选扩展

1. **文章评论** - 复用现有讨论区功能
2. **文章标签** - 添加标签系统
3. **文章搜索** - 全文搜索功能
4. **RSS 订阅** - 生成 RSS feed
5. **文章统计** - 阅读量、点赞等

---

*文档版本: 2.0*
*创建日期: 2026-03-21*
*更新说明: 改为统一部署方案，博客功能集成到 React 项目中*
